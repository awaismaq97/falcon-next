# Falcon backend (FastAPI) — pinned to the versions verified in the `falcon`
# conda env. Ceilings on the fast-moving LLM libs guard against breaking API
# changes; floors match what the code was written and tested against.

# ── Web framework ──
fastapi==0.139.0
uvicorn[standard]==0.49.0
sse-starlette==3.4.5
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.14.2

# ── Config / secrets ──
python-dotenv==1.2.2
PyYAML==6.0.3

# ── LLM + data ──
openai>=2.41,<3
pymongo==4.7.3
requests>=2.32,<3

# ── Document extraction (chat doc upload: PDF / Word / Excel / PowerPoint) ──
pypdf==6.14.2
python-docx==1.2.0
openpyxl==3.1.5
python-pptx==1.0.2

# ── Tools agent (LangChain / LangGraph — weather + NASA APOD) ──
langchain>=1.3,<2
langchain-core>=1.4,<2
langchain-openai>=1.3,<2
langgraph>=1.2,<2

# ── Testing (optional; used by the Testing tab harness + unit tests) ──
hypothesis>=6.100
pytest>=8.0
mongomock>=4.1
